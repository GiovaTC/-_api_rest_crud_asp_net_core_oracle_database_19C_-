CREATE TABLE CLIENTES_AC (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NOMBRE VARCHAR2(100),
    EMAIL VARCHAR2(150),
    TELEFONO VARCHAR2(50),
    FECHA_REGISTRO DATE DEFAULT SYSDATE,
    CONSTRAINT PK_CLIENTES_AC PRIMARY KEY(ID)
);

COMMIT;